# When false, all api/*.ts modules make real Axios calls to VITE_API_BASE_URL
# instead of returning mock data. Defaults to true (mock mode) if unset.
VITE_MOCK_MODE=true

# Base URL of LungInsight-Backend once it's deployed.
VITE_API_BASE_URL=http://localhost:8000
