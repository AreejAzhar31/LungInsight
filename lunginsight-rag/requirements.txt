# Embeddings
sentence-transformers>=2.7.0
torch>=2.2.0            # sentence-transformers backend (CPU build is sufficient)

# Vector store (pick one; both listed so either backend works out of the box)
faiss-cpu>=1.8.0
chromadb>=0.5.0

# LLM
groq>=0.9.0

# Agent orchestration
langgraph>=0.2.0
langchain-core>=0.2.0   # transitive dependency LangGraph expects available

# Utilities
numpy>=1.26.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-mock>=3.14.0

# HTTP service wrapper
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
